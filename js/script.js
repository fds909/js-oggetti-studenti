const studente = {nome:"Fabio", cognome:"Fazio", età:"66"};

for (nome in studente) {
    console.log();
}

const studenti = ["Mauro", "Patrizia", "Gianfranco"];

/* Ciclare su tutti gli studenti e stampare per ognuno di essi, nome e cognome. */
for (let i = 0; i < studenti.length; i++) {
    console.log(studenti[i]);
  }

/* 3 prompt() per aggiungere un nuovo oggetto studente inserendo nell’ordine: nome, cognome e età. */





